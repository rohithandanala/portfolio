'use server';

/**
 * @fileOverview This file is intentionally left empty.
 * The portfolio chatbot functionality is handled by a live API endpoint,
 * so no client-side Genkit flow is required for the RAG system.
 */
